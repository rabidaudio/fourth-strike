h1.is-size-2 Merch

.block
  .field
    a href=iam8bit_sales_path
      span iam8bit

= render 'unfulfilled'

section.section
  = render_pagination(@merch)

  table.table
    thead
      tr
        th SKU
        th Name
        th Price
    tbody
      - @merch.each do |merch|
        tr
          td
            code = merch.sku
          td
            a href=merch_path(merch) = merch.name
            - if merch.album
              br
              span.is-size-7
                a href=album_path(merch.album) = merch.album.name
                - if merch.artist_name
                  span = ' by '
                  a href=merch_index_path(artist: merch.artist_name) = merch.artist_name
          td = merch.list_price.format

  = render_pagination(@merch)
